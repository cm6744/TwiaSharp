--Twia 2024.1

--Export the script, means it will be compiled and stored in global scope.
--We can import this file in any other script compiled after this.
export fib;

function fib(n) do
	if(n <= 2) do return 1; end
	return fib(n - 1) + fib(n - 2);
end